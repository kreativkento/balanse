/**
 * Hover motion utilities (see theme.css).
 * Uses ease-in-out for both grow and shrink.
 */

/** Card lift + subtle grow on hover (pricing cards, discipline cards, etc.) */
export const CARD_HOVER_GROW = 'motion-card-hover-grow';

/** Image zoom inside a hover group (gallery photos) */
export const IMAGE_HOVER_ZOOM = 'motion-image-hover-zoom';

/** Icon grow inside a hover group (upload/plus buttons) */
export const ICON_HOVER_GROW = 'motion-icon-hover-grow';

/** Base smooth transform transition for interactive scale hovers */
export const HOVER_SCALE_SMOOTH = 'motion-hover-scale-smooth';

/** Fade + slide up reveal on group hover */
export const HOVER_FADE_UP = 'motion-hover-fade-up';
